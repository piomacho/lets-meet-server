

export const makeRelations = () => {

    // example

    // Latests.hasMany(Comments, {
    //     sourceKey: 'id',
    //     foreignKey: 'latest_id',
    //     as: 'comments'
    // });
    
    // Comments.belongsTo(Latests, {
    //     foreignKey: 'latest_id',
    //     as: 'latests'
    // });
}